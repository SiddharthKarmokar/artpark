# environment starter data

3 stations, 10 days. A local station feed (`station_obs_local.csv`, °C, decimal humidity 0–100) and a third-party weather API export (`weather_api_export.csv`, °F, humidity 0–1) covering the same stations. The registry (`stations.csv`) is the authoritative source for station identity.
